import Note from '@/models/Note';

describe('Note.js', () => {
  it('getPayloadToAddNote should return an object with expected properties', () => {
    expect(true).toBe(true);
  });

  it('getPayloadToUpdateNote should return an object with expected properties', () => {
    expect(true).toBe(true);
  });
});
